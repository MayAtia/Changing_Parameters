<!DOCTYPE html>
<html>
<head>
  <title>Sensor Data</title>
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background-color: #1f2124;
      margin: 0;
      padding: 0;
      color: #fff;
    }

    .container {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    h1 {
      font-size: 2.5em;
      font-weight: 400;
      margin-bottom: 50px;
      color: #e6e6e6;
    }

    .card {
      background: #282c34;
      border-radius: 5px;
      width: 80%;
      max-width: 700px;
      padding: 20px;
      margin-bottom: 20px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
      transition: box-shadow 0.3s ease;
      list-style-type: none;
      display: flex;
      justify-content: space-between;
    }

    .card:hover {
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.5);
    }

    .card-title {
      font-size: 1.2em;
      font-weight: 500;
      color: #d8dee9;
    }

    .card-data {
      font-size: 1.2em;
      color: #88c0d0;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Most Recent Sensor Settings</h1>
    <div class="card">
      <div class="card-title">Light Sensor Delay</div>
      <div class="card-data"><%= lightSensorDelay %></div>
    </div>
    <div class="card">
      <div class="card-title">Relay Delay</div>
      <div class="card-data"><%= relayDelay %></div>
    </div>
    <div class="card">
      <div class="card-title">PWM Delay</div>
      <div class="card-data"><%= pwmDelay %></div>
    </div>
    <div class="card">
      <div class="card-title">Standby Delay</div>
      <div class="card-data"><%= standbyDelay %></div>
    </div>
  </div>
</body>
</html>
